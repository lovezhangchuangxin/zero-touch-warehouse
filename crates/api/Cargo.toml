[package]
name = "ztw-api"
version = "0.1.0"
edition = "2024"

[dependencies]
ztw-model = { path = "../model" }
ztw-sim = { path = "../sim" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
